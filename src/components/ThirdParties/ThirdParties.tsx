import React from "react";
import Page from "../Page";
import { Props } from "./ThirdParties.types";

const ThirdParties = (_props: Props) => {
  return <Page>ThirdParties</Page>;
};

export default React.memo(ThirdParties);
